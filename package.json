{
  "name": "pskeyword",
  "version": "1.1.0",
  "description": "Play Store Keyword Finder",
  "main": "src/main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron .",
    "build-osx": "electron-packager . 'PSKeyword' --platform=darwin --arch=x64 --out='build'",
    "build-win": "electron-packager . 'PSKeyword' --platform=win32 --arch=x64 --out='build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Teguh Arifianto",
  "license": "ISC",
  "devDependencies": {
    "electron": "^3.0.2",
    "electron-packager": "^12.1.2"
  },
  "dependencies": {
    "async": "^2.6.1",
    "google-play-scraper": "^6.2.0",
    "jquery": "^3.3.1"
  }
}
